import styles from "../styles/footer.module.css";

export const Footer = ({}) => {
  return (
    <div className={styles.main}>
      <h1>This is Footer.js</h1>
    </div>
  );
};

export default Footer;
